# ---------------------------------------------------------------------------
# RAG DEPENDENCIES
# ---------------------------------------------------------------------------
# These packages are required for RAG functionality (indexing and chat).
# They are imported lazily (inside function bodies) so the API can start
# without them. This is why they are in a separate file from the API deps.

chromadb>=0.5.0,<1.0.0     # Vector database for semantic search
ollama>=0.4.0,<1.0.0       # Python client for Ollama (embeddings + chat)
requests>=2.32.0,<3.0.0    # HTTP client (used by build_index_api.py)
